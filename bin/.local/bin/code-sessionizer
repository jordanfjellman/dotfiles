#!/bin/bash

find "$HOME/code" -mindepth 1 -maxdepth 3 \( -name .git -o -name .bare \) -print \
  | sed 's/\/\.git$//' \
  | sed 's/\/\.bare$//' \
  | sort -u \
  | fzf --select-1 \
  | tmux-sessionizer
